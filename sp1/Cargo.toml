[workspace]
members = [
  "fibonacci-guest",
  "sha2-guest",
  "ecdsa-guest",
  "transfer-eth-guest",
  "host",
]
resolver = "2"

[workspace.dependencies]
sp1-sdk = "5.0.7"
sp1-zkvm = "5.0.5"
sp1-build = "5.0.5"
utils = { path = "../utils" }
guests = { path = "../guests" }
dotenv = "0.15.0"
